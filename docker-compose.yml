version: "3"
services:
  postgres:
    container_name: local-postgres
    image: postgres
    ports:
      - 5432:5432
    hostname: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
